input {
  rabbitmq {
    host => "rabbitmq" # Use the RabbitMQ service name if running in Docker, otherwise the server address
    port => 5672
    user => "student" # Your RabbitMQ user
    password => "student1" # Your RabbitMQ password
    queue => "user_info_queue" # Your RabbitMQ queue name
    durable => true
    codec => "json"
  }
}

# Uncomment and adjust the filter section according to your data processing needs
#filter {
#  mutate {
#    add_field => { "source" => "rabbitmq" }
#  }
#}

output {
  elasticsearch {
    index => "logstash_user" #-%{+YYYY.MM.dd}"
    hosts => ["https://es01:9200"]
    user => "elastic"
    password => "student1"
    ssl_enabled => true
    cacert => "/usr/share/logstash/certs/ca/ca.crt"
  }
  #stdout {}
}
