<odoo>
  <data>
    <!-- explicit list view definition -->
<!--
    <record model="ir.ui.view" id="webhook.list">
      <field name="name">webhook list</field>
      <field name="model">webhook.webhook</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
-->

    <!-- actions opening views on models -->
<!--
    <record model="ir.actions.act_window" id="webhook.action_window">
      <field name="name">webhook window</field>
      <field name="res_model">webhook.webhook</field>
      <field name="view_mode">tree,form</field>
    </record>
-->

    <!-- server action to the one above -->
<!--
    <record model="ir.actions.server" id="webhook.action_server">
      <field name="name">webhook server</field>
      <field name="model_id" ref="model_webhook_webhook"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": model._name,
        }
      </field>
    </record>
-->

    <!-- Top menu item -->
<!--
    <menuitem name="webhook" id="webhook.menu_root"/>
-->
    <!-- menu categories -->
<!--
    <menuitem name="Menu 1" id="webhook.menu_1" parent="webhook.menu_root"/>
    <menuitem name="Menu 2" id="webhook.menu_2" parent="webhook.menu_root"/>
-->
    <!-- actions -->
<!--
    <menuitem name="List" id="webhook.menu_1_list" parent="webhook.menu_1"
              action="webhook.action_window"/>
    <menuitem name="Server to list" id="webhook" parent="webhook.menu_2"
              action="webhook.action_server"/>
-->
  </data>
</odoo>
